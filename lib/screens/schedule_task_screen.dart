import 'package:flutter/material.dart';

class ScheduleTaskScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Schedule Tasks'),
      ),
      body: Center(
        child: Text('Task Scheduling Interface'),
      ),
    );
  }
}
